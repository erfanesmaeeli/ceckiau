# from django import forms
# from app_account.models import Profile



# # class SignUpForm(forms.ModelForm):
# # 	# first_name = forms.CharField(max_length=30, required=False, help_text='can not be NULL !')
# #  #    last_name = forms.CharField(max_length=30, required=False, help_text='can not be NULL !')
# #  #    join_uni = forms.PositiveSmallIntegerField( help_text='can not be NULL !')
# #  #    email = forms.EmailField(max_length=254, help_text='Required. Inform a valid email address.')

# #     class Meta:
# #         model = Profile
# #         fields = (__all__)